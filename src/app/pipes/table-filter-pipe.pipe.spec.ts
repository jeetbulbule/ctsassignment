import { TableFilterPipePipe } from './table-filter-pipe.pipe';

describe('TableFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TableFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
